# Example environment variables - copy to .env and fill in your values
# NEVER commit the actual .env file to git

# === Core Configuration ===
FLASK_ENV=production
LOG_LEVEL=WARNING
ENABLE_FILE_LOGGING=0

# === API Configuration ===
FRONTEND_URL=https://your-frontend.vercel.app
OPENAI_API_KEY=your-openai-key-here

# === Rate Limiting ===
FREE_MESSAGES_PER_IP=2
FREE_QUOTA_WINDOW_SECONDS=86400
QUOTA_GPT5_PER_IP=3
QUOTA_GPT5_MINI_PER_IP=10

# === Celery/Redis (for async jobs) ===
ENABLE_ASYNC_JOBS=1
REDIS_URL=redis://localhost:6379/0
CELERY_RESULT_EXPIRES=86400

# === Security ===
ENFORCE_HTTPS=1
REQUIRE_API_KEY=0

# === Heroku Specific ===
# These are automatically set by Heroku, don't set manually:
# PORT
# DATABASE_URL

# === Vercel Specific ===
# These are automatically set by Vercel:
# VERCEL_URL
# VERCEL_ENV
